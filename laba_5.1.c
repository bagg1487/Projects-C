#include <stdio.h>
//Найти номер первого (последнего) справа единичного бита в целом числе a.
int f(unsigned int a){
    unsigned int b = a;
    int count =0;
    while ((b&1)==0){ //пока последний бит 0, продолжаем искать 1
		b = b>>1;//сдвигаем, пока не наткнемся на 1
		count++; //счетчик номера
	} return count;//возвращаем номер
    }
int main(){
    unsigned int a; // создаем переменную
    scanf("%u", &a);//считываем значение переменной
    printf("%d \n",f(a));//выводим на экран резулдьтат
}
